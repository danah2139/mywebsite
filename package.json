{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://danah2139.github.io/mywebsite",
  "dependencies": {
    "gh-pages": "^2.0.1",
    "nib": "^1.1.2",
    "prop-types": "^15.7.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-rotation": "^1.3.0",
    "react-scripts": "3.0.1",
    "styl": "^0.2.9",
    "stylus": "^0.54.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "yarn run build && gh-pages -d build",
    "eject": "react-scripts eject",
    "stylus": "stylus -u nib -c -w -o public/css/style.css src/index.styl"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
